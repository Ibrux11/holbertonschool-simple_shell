


#define PROMPT "@cisfun$"

int main(void)
    char *buffer;
    int buffer_size = 1024;
    buffer = ((char *)malloc(buffer_size * size_of(char))); 
    char*input;
    int status;

while(1)
{
    printf(PROMPT);
    input=
}
free(buffer);